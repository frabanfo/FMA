# Dynamic Asset Allocation under AI-Concentration Risk - dependencies
# Minimum tested versions; pin exact versions for strict reproducibility.
numpy>=1.24
pandas>=2.0
scipy>=1.10            # SLSQP optimizer, as in the course notebooks
scikit-learn>=1.3      # LedoitWolf shrinkage covariance
yfinance>=0.2.40       # ETF price download (auto_adjust=True -> total return)
pyarrow>=12.0          # frozen parquet data cache
matplotlib>=3.7        # charts
seaborn>=0.12          # correlation heatmaps / chart style
jupyter>=1.0           # run the notebooks
nbconvert>=7.0         # headless execution (jupyter nbconvert --execute)
